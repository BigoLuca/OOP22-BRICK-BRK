package brickbreaker.common;

/**
 * Enum of possible state game.
 */
public enum State {

    /** Pause. */
    PAUSE,
    /** Playing. */
    PLAYING,
    /** Win. */
    WIN,
    /** Lost. */
    LOST,
    /** Quit. */
    QUIT,
    /** Wait */
    WAIT
}
